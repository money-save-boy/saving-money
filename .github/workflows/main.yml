name: FTP Deploy
on:
  push:
    branches:
      - 'integration'
  pull_request:
    branches:
      - 'integration'
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ftp.lolipop.jp
          username: verse.jp-aso2201030
          password: Pass1023
